# CHANGELOG — tcm-moxibustion

版本号写在 `SKILL.md` frontmatter 的 `version` 字段，三者必须同步：
`SKILL.md 的 version` = 本文件的顶层条目 = git tag（如 `v1.0.0`）。

## 版本号规则

| 变更类型 | 升位 | 例子 |
|---|---|---|
| 重建数据、改数据结构、破坏已有命令/字段 | MAJOR | 换数据源、`--json` 字段改名、症状分类改名 |
| 新增子命令、新增字段（向后兼容） | MINOR | 加 `point-list`、加 `--trad` |
| 修 bug、改文案、补文档 | PATCH | 穴名错字修正、SKILL.md 措辞 |

改完记得做三件事：同步 frontmatter 版本号 → 在本文件顶部加条目 → 跑一遍全量回归（见文末）。

---

## [1.0.3] — 2026-09-18

### 修复：复现流水线不完整（原声称「可完整复现」不成立）

核查时发现文档里的复现步骤漏了环节，实际跑不出线上数据：

1. **缺失 `build_skill_data.py`** —— 该脚本是「原始条目 → skill 数据」的必要环节，
   却从未进仓库（只在临时工作区）。已补入。
2. **顺序不完整** —— 文档只列 4 步，实际需要 8 步（漏了 `build_skill_data.py`、
   `fix_pua.py`、`clean_v2.py`）。
3. **复现结果差 6 条** —— `clean_v2.py` 只按正则剔除噪声，漏掉 6 条人工判定项
   （`二伏兔`/`名醫治法`/`小商`/`治病要穴`/`絲竹`/`艾葉`：截断名、章节标题、药名），
   导致复现出 457 而非线上 451。现在 `clean_v2.py` 改读 `moxa_forbidden.json` 的
   `noise_dropped` 清单，**人工判断沉淀为数据**，复现精确一致。

### 新增
- **`scripts/run_pipeline.sh`** —— 一条命令跑完 8 阶段（含进度提示与末尾自检）。
- **提取脚本可移植**：路径改由环境变量覆盖，不再硬编码绝对路径
  - `TCM_WORKSPACE`（默认 `/tmp/tcm-src`）—— 语料与中间产物
  - `TCM_DATA`（默认 `<repo>/data`）—— 目标数据目录
  - `TCM_OPENCC_DIR`（默认 `/tmp/occdir`）—— opencc 安装位置
  - 好处：可在隔离目录测试流水线，**不会误改线上数据**
- **validate_skill.py §16**：断言提取脚本无 `Path()` 硬编码绝对路径、
  支持环境变量覆盖、流水线 8 阶段齐全。

### 验证
- 端到端隔离复现（`TCM_DATA=/tmp/repro3`）：`acupoints.json`（451 条）、
  `moxa_forbidden.json`、`symptoms.json` 与线上**逐字段完全一致，差异 0 处**
- 线上数据未被测试触碰
- 完整验证 60 项全通过

---

## [1.0.2] — 2026-09-18

### 新增
- **SKILL.md §5「成本纪律」**：实测各命令的 token 成本，明确要求「一律用
  `query.py`，绝不直接读 `data/*.json`」——直接读 `acupoints.json` 约
  **129,966 token**，而单穴查询只要 **232 token**，差约 56 倍。
- **validate_skill.py §15 文档一致性断言**：校验文档中的穴位数/灸壮数与实际数据
  一致、且版本号三元同步，防止文档随数据变动而飘移。

### 修复
- `references/03-classical-sources.md` 数据规模表仍写 469/334（未随噪声清理更新），
  已更正为 **451/327**。此缺陷正是由新增的 §15 断言发现。

---

## [1.0.1] — 2026-09-18

### 修复
- **`install.sh` 易失目录守卫**：从 `/tmp` 等易失位置运行时，脚本原会把已有真实
  skill 目录移成 `.bak`、并把各 agent 的软链指向该易失位置 —— 目录被清理后 skill
  即失效。现在会检测并在「已有真实安装会被顶替」时**直接拒绝**（exit 1）并给出
  正确用法；无真实安装可顶替时打印警告后继续；可用 `FORCE_VOLATILE=1` 强行绕过。
  持久目录（如 `~/.hermes/skills/`、`~/skills/`）运行不受影响。

> 该缺陷由发布前实测发现：从 `/tmp` 全新 clone 后跑 `install.sh`，
> 真实安装被顶替为指向 `/tmp` 的软链。

### 验证
- 守卫拒绝场景：exit 1，真实安装零改动，无遗留 `.bak`
- `FORCE_VOLATILE=1` 覆盖场景：按预期强行继续
- 持久目录正常场景：行为不变，幂等
- 47 项回归全通过

---

## [1.0.0] — 2026-09-18

首个发布版本。

### 数据
- **451 个穴位**（327 个载有灸壮数）：解析《针灸大成》卷八/九「考正穴法」、卷十「神应经」、卷十一「治症总要」
- **15 类症状**、1542 处古文献命中（889 处含灸法）：脚冷/手足冰凉、腰痛/腰酸/肾虚、胃寒/胃痛/腹胀、腹泻/久泄、宫寒/痛经/月经、失眠/多梦、咳嗽/哮喘/痰、体虚/易感冒/气虚、关节冷痛/风湿、夜尿/尿频/遗尿、疲劳/阳虚体质、头痛/头晕、肩颈/背痛、发背/痈疽/疮、中风/急救
- **禁灸穴分类**：明确禁灸 17 穴 / 典籍分歧 11 穴（另有 6 条解析噪声已剔除并保留审计记录）
- 数据源：维基文库公版《针灸大成》全 12 卷、《备急灸法》（宋）、《扁鹊心书》（宋）、《外台秘要》（唐）

### 命令
- `symptom <症状>` / `symptom-list` —— 症状反查取穴，输出古籍原文
- `point <穴名...>` —— 穴位详情（定位 / 灸壮 / 禁灸 / 原文）
- `point-list [关键词]` —— 按名或定位筛穴位
- `moxa-list` —— 所有有灸壮数且未禁灸的穴位
- `forbidden [disputed]` —— 禁灸穴，区分「明确」与「典籍分歧」
- `--trad` —— 输出繁体原文（默认简体）

### 简体层
- 预生成简体变体存入 `_s` 字段（`name_s` / `location_s` / `text_s` / `ctx_s` / `consequence_s` / `forbidden_by_s`），**零运行时依赖**
- 用 opencc（`t2s`，词组感知）转换；仅构建期需要，不进运行依赖
- 中医专有字人工修正：`太谿→太溪`、`後谿→后溪`、`齗交→龈交`；`譩𫍻→譩譆` 回退
- 输入简繁皆可（内部建双向索引）

### 数据修复（首版前）
- 清理 **18 条非穴位噪声**（469→451）：`灸卒死` / `灸疝痛` / `灸翻胃` 等治法条目、`捷要灸法` 等章节标题、`故诸邪皆` / `甲木之腑` 等散文片段；已确认其引用的真实穴位（神阙 / 肾俞 / 风池 / 印堂）仍在库中
- 修复 **558 处 Unicode 私用区字符（PUA）**——维基文库字体缺字导致，会静默污染查询；替换为可见标记 `□`（共 140 处），穴位名无一受损
- 补全《针灸大成》卷九/卷十一解析范围，补齐命门 / 中脘 / 太冲等关键穴

### 文档
- `SKILL.md` —— 速查表 + 安全边界（脚冷 / 腰痛直接结论）
- `references/01-moxa-methods.md` —— 灸法操作、壮数换算、施灸流程
- `references/02-safety-contraindications.md` —— 完整禁忌清单、高危人群、红旗症状、水泡处理
- `references/03-classical-sources.md` —— 数据出处、提取方法、简繁处理、已知局限、复现步骤

### 工具
- `scripts/validate_skill.py` —— 47 项回归验证
- `scripts/extraction/` —— 9 个提取脚本（可完整复现数据）
- `install.sh` / `uninstall.sh` —— 跨 agent symlink 安装

### 已知局限
- 提取为**规则近似，非人工校勘**；重要决策建议回溯原文（引文均可回溯到维基文库对应卷次）
- 140 处缺字以 `□` 标记（维基文库字体缺字），不编造补字
- 无现代临床试验数据、无禁忌证更新、无个体化辨证

---

## 回归测试

改完数据或代码，跑一遍：

```bash
python3 scripts/validate_skill.py
```

预期 `结果: 全部通过 ✅`。该脚本含**防回归断言**，重点包括：

- 委中必须标记为禁灸（与网传「腰痛灸委中」相反，古籍明标「禁灸」）
- 简体字段无残留繁体
- 太谿→太溪、齗交→龈交 等中医专有字修正生效
- 无 PUA 私用区字符残留
- 无「灸XX」治法条目混入穴位库
- 简繁输入双向可用
